export default (node) => {
    // console.log(node);
    // console.log(node.className);
    // const content = document.querySelector(`.${node.className}`);
    // while (content.firstChild) {
    //     content.removeChild(content.lastChild);
    // }
};